{
  "version": 2,
  "buildCommand": "cd packages/landing && npm install && npm run build",
  "outputDirectory": "packages/landing/out",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
} 